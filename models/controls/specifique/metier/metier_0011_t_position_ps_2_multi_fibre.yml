version: 2

models:
  - name: metier_0011_t_position_ps_2_multi_fibre
    description: |
      Controle qu'une fibre n'est pas presente en aval dans plusieurs positions (ps_2).
      Detecte les erreurs de route optique sur t_position.ps_2.
    config:
      tags: ['control', 'métier']
